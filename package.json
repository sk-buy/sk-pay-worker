{
  "name": "@sk-buy/pay-worker",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260523.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.18.0"
  },
  "cloudflare": {
    "bindings": {
      "ADMIN_TOKEN": {
        "description": "Worker 设置页面管理口令。部署完成后通过 `/admin?token=...` 打开设置页面。"
      },
      "PAY_CONFIG": {
        "description": "保存 EPay 参数和验证文件内容的 KV Namespace。"
      }
    }
  }
}
